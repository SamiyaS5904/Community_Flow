# CommunityFlow — Python dependencies
#
# Everything listed here is imported somewhere in the codebase; a test walks
# every import against this file so the two cannot drift apart.
# ====================================================

# Core LLM and content generation
openai>=1.0.0


# Database
SQLAlchemy>=2.0.0
psycopg[binary]>=3.1.0
pgvector>=0.2.5

# HTTP client for Telegram and Serper APIs
httpx>=0.27.0

# Configuration and data validation
python-dotenv>=1.0.0

# YAML config parsing
PyYAML>=6.0.0

# Dashboard
Flask>=3.0.0
Pillow>=10.0.0


# Retry logic for API calls
tenacity>=8.0.0

# Background task scheduler
APScheduler>=3.10.0

# Testing
pytest>=7.0.0

# Type hints backport (Python 3.9 compat)
typing-extensions>=4.0.0

# Headless rendering for PNG/PDF templates
playwright>=1.40.0

# Production WSGI server for Render deployment
gunicorn>=21.0.0
watchdog>=3.0.0                 # Flask's auto-reloader uses it in development
